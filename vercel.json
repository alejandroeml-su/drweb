{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "proyectos/avante/dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
